# Netlify Preview Trigger

This marker commit exists only to trigger the first Netlify branch deployment for `feat/contextual-work-budget-inputs` after connecting the repository and configuring the public Supabase environment variables.

It has no runtime effect on Arkan Al Makan.
