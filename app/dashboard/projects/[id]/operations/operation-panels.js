'use client';

import { useCallback, useEffect, useMemo, useState } from 'react';
import { supabase } from '@/lib/supabase';
import { receiptLabel } from '@/lib/operation-safety.mjs';
import { saveOperationWithQueue } from '@/lib/verified-operation-write';
import styles from './operations.module.css';

const CATEGORIES = ['وجبات','أجور','ترحيل','سكن','عدد وأدوات','سقالات','مواد','وقود','وقود ومحروقات','تأمين مسترد','عهدة','تأمين طبي','ضيافة','أخرى'];
const PAYER_AR = { contractor:'المقاول', arkan_custody:'أركان من العهدة', arkan_direct:'أركان مباشرة', employee:'موظف من ماله الخاص' };
const CHARGE_AR = { arkan:'أركان', contractor:'المقاول', owner:'المالك' };
const SOURCE_AR = { bank:'تحويل بنكي', cash:'نقدًا', custody:'من عهدة' };
const money = (n) => Number(n || 0).toLocaleString('en-US', { maximumFractionDigits: 2 });

async function verifiedWrite({ operation, projectId, date, payload, onQueueChange }) {
  const result = await saveOperationWithQueue({ operation, projectId, workDate:date, payload, batchId:null, sourceKind:'live', sourceRef:null, certainty:'confirmed' });
  onQueueChange?.(result.pendingCount || 0);
  return result;
}
function Feedback({ value }) { if (!value) return null; return <div className={value.type === 'error' ? styles.panelError : styles.panelSuccess}>{value.text}</div>; }
function PanelEmpty({ children }) { return <div className={styles.panelEmpty}>{children}</div>; }

export function OutputPanel({ projectId, date, contractor, onQueueChange }) {
  const [items,setItems]=useState([]),[links,setLinks]=useState([]),[rows,setRows]=useState([]);
  const [form,setForm]=useState({item_id:'',qty:'',notes:''}); const [loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[feedback,setFeedback]=useState(null);
  const load=useCallback(async()=>{if(!projectId||!date||!contractor?.id)return;setLoading(true);setFeedback(null);try{const [itemsQ,dayQ,linksQ]=await Promise.all([supabase.from('project_items').select('id,description_ar,unit,sort_order').eq('project_id',projectId).eq('kind','item').order('sort_order'),supabase.from('timesheet_days').select('id').eq('project_id',projectId).eq('work_date',date).maybeSingle(),supabase.from('v_item_assignments').select('project_item_id,contractor_id,start_date,end_date,is_active').eq('project_id',projectId).eq('contractor_id',contractor.id)]);if(itemsQ.error)throw itemsQ.error;if(dayQ.error)throw dayQ.error;setItems(itemsQ.data||[]);setLinks(linksQ.error?[]:(linksQ.data||[]));if(dayQ.data?.id){const q=await supabase.from('day_items').select('id,project_item_id,contractor_id,group_output,unit,notes').eq('day_id',dayQ.data.id).eq('contractor_id',contractor.id);if(q.error)throw q.error;setRows(q.data||[])}else setRows([])}catch(e){setFeedback({type:'error',text:'تعذر تحميل إنجاز اليوم: '+(e.message||e)})}setLoading(false)},[projectId,date,contractor?.id]);
  useEffect(()=>{load()},[load]);
  const availableItems=useMemo(()=>{const active=links.filter(x=>x.is_active!==false&&(!x.start_date||x.start_date<=date)&&(!x.end_date||x.end_date>=date));if(!active.length)return items;const ids=new Set(active.map(x=>x.project_item_id));return items.filter(x=>ids.has(x.id))},[items,links,date]);
  useEffect(()=>{if(!availableItems.length)return;setForm(c=>availableItems.some(x=>x.id===c.item_id)?c:{...c,item_id:availableItems[0].id})},[availableItems]);
  async function save(e){e.preventDefault();const item=availableItems.find(x=>x.id===form.item_id);if(!item||!Number(form.qty))return;setBusy(true);setFeedback(null);try{const result=await verifiedWrite({operation:'output',projectId,date,onQueueChange,payload:{contractor_id:contractor.id,item_id:item.id,qty:Number(form.qty),unit:item.unit||null,notes:form.notes||null}});setFeedback({type:'success',text:result.status==='queued'?'حُفظ الإنجاز على هذا الجهاز وينتظر الاتصال.':`تم حفظ الإنجاز — ${receiptLabel(result.receipt)}`});setForm(c=>({...c,qty:'',notes:''}));if(result.status==='verified')await load()}catch(e){setFeedback({type:'error',text:'تعذر حفظ الإنجاز: '+(e.message||e)})}setBusy(false)}
  const totalQty=rows.reduce((s,r)=>s+Number(r.group_output||0),0);
  return <section className={styles.operationGrid}><main className={styles.formPane}><div className={styles.panelTitle}><div><span>DAILY OUTPUT</span><h2>الإنجاز اليومي</h2><p>سجّل الكمية المنفذة اليوم على البند المرتبط بالمقاول.</p></div><strong>{rows.length}</strong></div><Feedback value={feedback}/>{loading?<PanelEmpty>جارٍ تحميل البنود…</PanelEmpty>:!availableItems.length?<PanelEmpty>لا توجد بنود متاحة لهذا المقاول في المشروع.</PanelEmpty>:<form className={styles.operationForm} onSubmit={save}><label className={styles.wideField}><span>البند</span><select value={form.item_id} onChange={e=>setForm(f=>({...f,item_id:e.target.value}))}>{availableItems.map(item=><option key={item.id} value={item.id}>{item.description_ar} — {item.unit||'وحدة'}</option>)}</select></label><label><span>الكمية المنفذة</span><input autoFocus required type="number" min="0" step="any" value={form.qty} onChange={e=>setForm(f=>({...f,qty:e.target.value}))}/></label><label><span>ملاحظة</span><input value={form.notes} onChange={e=>setForm(f=>({...f,notes:e.target.value}))}/></label><button className={styles.primaryAction} disabled={busy}>{busy?'جارٍ الحفظ…':'حفظ الإنجاز'}</button></form>}</main><aside className={styles.historyPane}><div className={styles.historyHead}><div><span>اليوم</span><strong>{contractor?.name_ar}</strong></div><b>{money(totalQty)}</b></div><div className={styles.activityList}>{rows.length?rows.map(row=>{const item=items.find(x=>x.id===row.project_item_id);return <div className={styles.activityRow} key={row.id}><div><strong>{item?.description_ar||'بند'}</strong><small>{row.notes||'بدون ملاحظة'}</small></div><b>{money(row.group_output)} {row.unit||item?.unit||''}</b></div>}):<PanelEmpty>لا يوجد إنجاز مسجل لهذا المقاول اليوم.</PanelEmpty>}</div></aside></section>;
}

export function ExpensePanel({ projectId, date, contractor, onQueueChange }) {
  const empty={amount:'',notes:'',category:'مواد',payer:'contractor',charge_to:'arkan',is_recoverable:false,project_item_id:'',paid_by_employee_id:'',beneficiary_kind:'contractor',expense_date:date};
  const [items,setItems]=useState([]),[rows,setRows]=useState([]),[employees,setEmployees]=useState([]),[form,setForm]=useState(empty),[editingId,setEditingId]=useState('');
  const [loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[feedback,setFeedback]=useState(null);
  useEffect(()=>{if(!editingId)setForm(f=>({...f,expense_date:date}))},[date,editingId]);
  const load=useCallback(async()=>{if(!projectId||!date||!contractor?.id)return;setLoading(true);setFeedback(null);try{const [itemsQ,rowsQ,empQ]=await Promise.all([supabase.from('project_items').select('id,description_ar').eq('project_id',projectId).eq('kind','item').order('sort_order'),supabase.from('contractor_expenses').select('id,expense_date,category,amount,notes,is_recoverable,payer,charge_to,project_item_id,paid_by_employee_id,beneficiary_kind,beneficiary_contractor_id,reimbursement_status').eq('project_id',projectId).eq('contractor_id',contractor.id).eq('expense_date',date).order('created_at'),supabase.from('employees').select('id,full_name').eq('is_active',true).order('full_name')]);const error=[itemsQ,rowsQ,empQ].find(x=>x.error)?.error;if(error)throw error;setItems(itemsQ.data||[]);setRows(rowsQ.data||[]);setEmployees(empQ.data||[])}catch(e){setFeedback({type:'error',text:'تعذر تحميل مصروفات اليوم: '+(e.message||e)})}setLoading(false)},[projectId,date,contractor?.id]);
  useEffect(()=>{load()},[load]);
  function startEdit(row){setEditingId(row.id);setForm({amount:String(row.amount||''),notes:row.notes||'',category:row.category||'أخرى',payer:row.paid_by_employee_id?'employee':(row.payer||'contractor'),charge_to:row.charge_to||'arkan',is_recoverable:!!row.is_recoverable,project_item_id:row.project_item_id||'',paid_by_employee_id:row.paid_by_employee_id||'',beneficiary_kind:row.beneficiary_kind||'contractor',expense_date:row.expense_date||date});setFeedback(null)}
  function cancelEdit(){setEditingId('');setForm({...empty,expense_date:date})}
  async function save(e){e.preventDefault();if(!Number(form.amount)||!form.notes.trim())return;setBusy(true);setFeedback(null);try{if(editingId){const {error}=await supabase.rpc('fn_update_project_expense',{p_expense_id:editingId,p_expense_date:form.expense_date,p_amount:Number(form.amount),p_category:form.category,p_notes:form.notes.trim(),p_project_item_id:form.project_item_id||null,p_paid_by_employee_id:form.payer==='employee'?(form.paid_by_employee_id||null):null,p_beneficiary_kind:form.beneficiary_kind||'contractor',p_beneficiary_contractor_id:form.beneficiary_kind==='contractor'?contractor.id:null});if(error)throw error;setFeedback({type:'success',text:'تم تعديل المصروف وحفظ التصحيح في سجل التدقيق.'});cancelEdit();await load()}else{if(form.payer==='employee'){setFeedback({type:'error',text:'حفظ مصروف مدفوع من موظف يحتاج تحديث قاعدة البيانات الجديد أولاً.'});setBusy(false);return}const recoverable=form.payer!=='contractor'&&!!form.is_recoverable;const result=await verifiedWrite({operation:'expense',projectId,date,onQueueChange,payload:{contractor_id:contractor.id,amount:Number(form.amount),category:form.category,payer:form.payer,charge_to:form.charge_to,is_recoverable:recoverable,project_item_id:recoverable?null:(form.project_item_id||null),notes:form.notes.trim()}});setFeedback({type:'success',text:result.status==='queued'?'حُفظ المصروف على الجهاز وينتظر الاتصال.':`تم حفظ المصروف — ${receiptLabel(result.receipt)}`});setForm({...empty,expense_date:date});if(result.status==='verified')await load()}}catch(e){setFeedback({type:'error',text:'تعذر حفظ المصروف: '+(e.message||e)})}setBusy(false)}
  const total=rows.reduce((s,r)=>s+Number(r.amount||0),0);
  return <section className={styles.operationGrid}><main className={styles.formPane}><div className={styles.panelTitle}><div><span>EXPENSES</span><h2>{editingId?'تعديل المصروف':'المصروفات'}</h2><p>البند اختياري. ويمكن تصحيح التاريخ والبيان والمبلغ بعد التسجيل مع بقاء سجل التدقيق.</p></div><strong>{money(total)} <small>ر.س</small></strong></div><Feedback value={feedback}/>{loading?<PanelEmpty>جارٍ تحميل مصروفات اليوم…</PanelEmpty>:<form className={styles.operationForm} onSubmit={save}><label><span>تاريخ المصروف</span><input required type="date" value={form.expense_date} onChange={e=>setForm(f=>({...f,expense_date:e.target.value}))}/></label><label><span>المبلغ</span><input required type="number" min="0.01" step="0.01" value={form.amount} onChange={e=>setForm(f=>({...f,amount:e.target.value}))}/></label><label><span>التصنيف</span><select value={form.category} onChange={e=>setForm(f=>({...f,category:e.target.value}))}>{CATEGORIES.map(x=><option key={x}>{x}</option>)}</select></label><label className={styles.wideField}><span>البيان</span><input required value={form.notes} onChange={e=>setForm(f=>({...f,notes:e.target.value}))}/></label><label><span>من دفع؟</span><select value={form.payer} onChange={e=>setForm(f=>({...f,payer:e.target.value,paid_by_employee_id:e.target.value==='employee'?f.paid_by_employee_id:''}))}>{Object.entries(PAYER_AR).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></label>{form.payer==='employee'&&<label><span>الموظف الدافع</span><select required value={form.paid_by_employee_id} onChange={e=>setForm(f=>({...f,paid_by_employee_id:e.target.value}))}><option value="">اختر الموظف</option>{employees.map(x=><option key={x.id} value={x.id}>{x.full_name}</option>)}</select></label>}<label><span>على من؟</span><select value={form.charge_to} onChange={e=>setForm(f=>({...f,charge_to:e.target.value}))}>{Object.entries(CHARGE_AR).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></label><label><span>البند</span><select value={form.project_item_id} onChange={e=>setForm(f=>({...f,project_item_id:e.target.value}))}><option value="">مصروف عام للمشروع — غير مرتبط ببند</option>{items.map(item=><option key={item.id} value={item.id}>{item.description_ar}</option>)}</select></label><button className={styles.primaryAction} disabled={busy}>{busy?'جارٍ الحفظ…':editingId?'حفظ التعديل':'حفظ المصروف'}</button>{editingId&&<button type="button" onClick={cancelEdit}>إلغاء التعديل</button>}</form>}</main><aside className={styles.historyPane}><div className={styles.historyHead}><div><span>مصروف اليوم</span><strong>{contractor?.name_ar}</strong></div><b>{money(total)} ر.س</b></div><div className={styles.activityList}>{rows.length?rows.map(row=><div className={styles.activityRow} key={row.id}><div><strong>{row.category}</strong><small>{row.notes||'—'} {row.project_item_id?'· مرتبط ببند':'· مصروف عام'} {row.reimbursement_status==='due'?'· مستحق لموظف':''}</small><button type="button" onClick={()=>startEdit(row)}>تعديل</button></div><b>{money(row.amount)} ر.س</b></div>):<PanelEmpty>لا توجد مصروفات لهذا المقاول اليوم.</PanelEmpty>}</div></aside></section>;
}

export function FinancePanel({ projectId, date, contractor, onQueueChange }) {
  const [kind,setKind]=useState('advance'),[advances,setAdvances]=useState([]),[payments,setPayments]=useState([]),[form,setForm]=useState({amount:'',notes:'',source:'bank',reference:''}),[busy,setBusy]=useState(false),[feedback,setFeedback]=useState(null);
  const load=useCallback(async()=>{if(!projectId||!date||!contractor?.id)return;setFeedback(null);try{const [a,p]=await Promise.all([supabase.from('contractor_advances').select('id,amount,remaining,notes').eq('project_id',projectId).eq('contractor_id',contractor.id).eq('advance_date',date).order('created_at'),supabase.from('contractor_payments').select('id,amount,kind,source,reference,notes').eq('project_id',projectId).eq('contractor_id',contractor.id).eq('payment_date',date).order('created_at')]);const error=[a,p].find(x=>x.error)?.error;if(error)throw error;setAdvances(a.data||[]);setPayments(p.data||[])}catch(e){setFeedback({type:'error',text:'تعذر تحميل السلف والدفعات: '+(e.message||e)})}},[projectId,date,contractor?.id]);useEffect(()=>{load()},[load]);
  async function save(e){e.preventDefault();if(!Number(form.amount))return;setBusy(true);setFeedback(null);try{const payload=kind==='advance'?{contractor_id:contractor.id,amount:Number(form.amount),notes:form.notes||null}:{contractor_id:contractor.id,amount:Number(form.amount),kind:'on_account',source:form.source,reference:form.reference||null,notes:form.notes||null};const result=await verifiedWrite({operation:kind,projectId,date,payload,onQueueChange});setFeedback({type:'success',text:result.status==='queued'?'حُفظت الحركة على الجهاز وتنتظر الاتصال.':`تم حفظ الحركة — ${receiptLabel(result.receipt)}`});setForm({amount:'',notes:'',source:'bank',reference:''});if(result.status==='verified')await load()}catch(e){setFeedback({type:'error',text:'تعذر حفظ الحركة: '+(e.message||e)})}setBusy(false)}
  const advanceTotal=advances.reduce((s,x)=>s+Number(x.amount||0),0),paymentTotal=payments.reduce((s,x)=>s+Number(x.amount||0),0);
  return <section className={styles.operationGrid}><main className={styles.formPane}><div className={styles.panelTitle}><div><span>ADVANCES / PAYMENTS</span><h2>السلف والدفعات</h2><p>سجّل السلفة أو الدفعة للمقاول من نفس سياق المشروع واليوم.</p></div><strong>{money(advanceTotal+paymentTotal)} <small>ر.س</small></strong></div><Feedback value={feedback}/><div className={styles.typeTabs}><button className={kind==='advance'?styles.typeOn:''} onClick={()=>setKind('advance')}>سلفة</button><button className={kind==='payment'?styles.typeOn:''} onClick={()=>setKind('payment')}>دفعة</button></div><form className={styles.operationForm} onSubmit={save}><label><span>المبلغ</span><input required type="number" min="0.01" step="0.01" value={form.amount} onChange={e=>setForm(f=>({...f,amount:e.target.value}))}/></label>{kind==='payment'&&<label><span>طريقة الدفع</span><select value={form.source} onChange={e=>setForm(f=>({...f,source:e.target.value}))}>{Object.entries(SOURCE_AR).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></label>}{kind==='payment'&&<label><span>المرجع</span><input value={form.reference} onChange={e=>setForm(f=>({...f,reference:e.target.value}))}/></label>}<label><span>ملاحظة</span><input value={form.notes} onChange={e=>setForm(f=>({...f,notes:e.target.value}))}/></label><button className={styles.primaryAction} disabled={busy}>{busy?'جارٍ الحفظ…':kind==='advance'?'حفظ السلفة':'حفظ الدفعة'}</button></form></main><aside className={styles.historyPane}><div className={styles.historyHead}><div><span>حركة اليوم</span><strong>{contractor?.name_ar}</strong></div><b>{advances.length+payments.length}</b></div><div className={styles.activityList}>{advances.map(row=><div className={styles.activityRow} key={`a-${row.id}`}><div><strong>سلفة</strong><small>{row.notes||'—'}</small></div><b>{money(row.amount)} ر.س</b></div>)}{payments.map(row=><div className={styles.activityRow} key={`p-${row.id}`}><div><strong>دفعة</strong><small>{row.notes||SOURCE_AR[row.source]||'—'}</small></div><b>{money(row.amount)} ر.س</b></div>)}{!advances.length&&!payments.length&&<PanelEmpty>لا توجد سلف أو دفعات لهذا اليوم.</PanelEmpty>}</div></aside></section>;
}
