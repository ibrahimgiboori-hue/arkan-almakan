# ملحق الربط الملزم لدستور المطبوعات — الإصدار 1.2

هذا الملحق جزء ملزم من دستور المطبوعات والمعاملات، ويضيف قاعدة الربط التقني المركزي دون تغيير القواعد البصرية والإجرائية السابقة.

## القاعدة العليا للربط
لا يعتبر أي مطبوع **Governed** لمجرد توافق شكله مع الدستور. لا بد أن يكون مربوطًا فعليًا بطبقة الدستور المركزية ومسجلًا في سجل المطبوعات البرمجي.

## مستويات التعديل
1. **Global**: تعديل عام لكل المطبوعات الخاضعة للدستور. ينفذ مرة واحدة في الطبقة المركزية.
2. **Family**: تعديل محدود لعائلة مترابطة مثل الموارد البشرية أو المستخلصات أو عروض الأسعار.
3. **Document Profile**: تعديل خاص بنوع مستند واحد فقط، بشرط ألا يتعارض مع القواعد العليا للدستور.

## شروط صفة Governed
لا ينتقل المطبوع إلى Governed إلا بعد تحقق جميع الآتي:
- وجود مفتاح فريد له في `lib/print-governance.js`.
- مروره عبر `ConstitutionPrintFrame` أو طبقة مركزية مكافئة معتمدة لاحقًا.
- اعتماده على قواعد الصفحة والهوية والشبكة والتنسيق المشتركة، لا نسخة محلية مكررة منها.
- عدم إعادة تعريف القواعد العامة في CSS خاص بالمستند؛ يسمح فقط بالفروق المسموح بها على مستوى Family أو Document Profile وبأسماء معزولة.
- اجتياز اختبار المطبوع وفق الدستور.
- تسجيل إصدار الدستور الذي اجتاز الاختبار عليه.

## نقطة التعديل الواحدة
التعديلات العامة يجب أن تمر من مكونات وقواعد مشتركة مثل:
- `components/print/PrintFrame.js`
- `components/print/ConstitutionPrintFrame.js`
- `app/print/print-system.css`
- `app/print/procedure-system.css`
- `lib/print-governance.js`

ولا يجوز لمطبوع Governed أن ينسخ هذه القواعد إلى ملف محلي ثم يعدلها بمعزل عن البقية.

## الربط البصري البرمجي
تضيف الوصلة المركزية لكل مطبوع خاضع طبقات تسمية ثابتة:
- `print-governed` للقواعد العامة.
- `print-family-<family>` لقواعد العائلة.
- `print-doc-<document>` للفروق الخاصة بالمستند.

وبذلك يمكن تنفيذ تعديل عام مرة واحدة، أو تعديل عائلة واحدة، أو تعديل مستند واحد دون كسر الدستور.

## قاعدة المسار
الأصل أن تكون النسخة الرسمية القابلة للطباعة تحت مسارات `/print/...` حتى ترث طبقة الطباعة المشتركة. أي شاشة تشغيلية أو عامة خارج `/print` تحتاج نسخة رسمية للطباعة يجب أن تربط بنسخة طباعة دستورية بدلاً من تحويل الشاشة نفسها إلى محرك طباعة مستقل.

## حالة الإصدار
- الدستور الأساسي: 1.1.
- هذا الملحق يرفع منظومة الحوكمة الفعلية إلى **1.2**.
- عند دمج النصوص لاحقًا في ملف واحد، يصبح `PRINT_GOVERNANCE.md` نفسه الإصدار 1.2 دون تغيير مضمون هذا الملحق.
