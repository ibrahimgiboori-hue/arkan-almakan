# سجل حوكمة المطبوعات — Audit Registry

**الحالة المرجعية: دستور 1.2**

هذا السجل يوضح حالة عائلات المطبوعات الحالية. الحالة البرمجية الرسمية موجودة في `lib/print-governance.js`، وهذا الملف شرح بشري موازي لها.

## Governed — خاضع بالكامل
لا توجد عائلة مصنفة Governed حتى الآن وفق التعريف الصارم الجديد. لا يمنح هذا التصنيف إلا بعد الربط المركزي + إزالة تكرار القواعد العامة + اجتياز اختبار الطباعة.

## Migrating — مربوط وجارٍ استكماله
### 1) طلب الإجازة
- المسار: `/print/leave/[id]`
- العائلة: HR
- يستخدم `PrintFrame` وشبكة Excel ومحرك إجراءات مشترك.
- المطلوب قبل Governed: تثبيت شكل منطقة الإجراءات وفق 1.1/1.2، اختبار الإلكتروني/اليدوي، اختبار الأسماء الطويلة والصفحة الواحدة والطباعة PDF.

### 2) دورة المستخلص
- المسارات: `/print/claims/[id]`, `/print/claim/[id]`, `/print/invoice-request/[id]`
- العائلة: Projects/Finance
- محرك واحد لأربعة مستندات: محضر قياس، مطالبة مالية، إشعار استلام، مذكرة طلب فاتورة.
- يستخدم `PrintFrame`، لكن ما زال لديه CSS محلي يعيد تعريف أجزاء من الشكل.
- المطلوب قبل Governed: تحويل CSS المحلي إلى فروق عائلة فقط واختبار الحالات الأربع.

## Legacy — مسجل لكنه خارج الحكم الكامل
### 3) عرض السعر / جدول الكميات
- المسار: `/print/quote/[id]`
- محرك تقسيم صفحات وCSS مستقل.
- يجب الحفاظ على خصائص تقسيم الصفحات، الترقيم الهرمي، وسحب الختم/التوقيع عند التحويل.

### 4) تقرير الموظفين
- المسار: `/print/employees`
- يبني A4 والترويسة والتذييل محليًا ويستخدم `emp-report.css`.

### 5) مجلس الإدارة والملاك
- المسار: `/print/board`
- يبني الصفحة محليًا ويشارك CSS تقرير الموظفين.

### 6) محرك المستندات العام
- المسار: `/print/[id]`
- يغطي قوالب متعددة وله `print.css` واسع مستقل.
- لا يحول دفعة واحدة؛ يقسم داخليًا إلى عائلات قوالب ثم تخضع كل عائلة للدستور.

### 7) العرض الوظيفي للمرشح
- المسار التشغيلي الحالي: `/offers/[token]`
- شاشة تفاعلية للقبول والتوقيع وليست نسخة طباعة رسمية.
- عند اعتماد الطباعة الرسمية ينشأ مسار `/print/...` مربوط بالدستور، وتبقى الشاشة العامة للتفاعل فقط.

### 8) مسودة عقد المرشح
- المسار التشغيلي الحالي: `/contracts/[token]`
- شاشة تفاعلية وليست نسخة طباعة رسمية.
- عند اعتماد الطباعة الرسمية ينشأ مسار `/print/...` مربوط بالدستور.

## قاعدة التدقيق الدائمة
- كل مسار تحت `/print` يمر عبر `PrintGovernanceBoundary`.
- المسار المسجل يأخذ `document`, `family`, `status`, `governance-version` آليًا.
- أي مسار `/print` غير موجود في Registry يصنف `unregistered` حتى يراجع، فلا يعتبر خاضعًا للدستور تلقائيًا.
- التعديلات العامة تطبق على `.print-governed` بعد اكتمال الهجرة، وتوجد طبقات `print-family-*` و`print-doc-*` للتعديلات المحدودة.

## ترتيب الهجرة المقترح
1. الإجازات والإجراءات.
2. دورة المستخلص كاملة.
3. تقارير الموارد البشرية: الموظفين ثم مجلس الإدارة.
4. عرض السعر وجدول الكميات.
5. محرك المستندات العام بحسب عائلات القوالب.
6. النسخ الرسمية للعروض الوظيفية ومسودات العقود.

يحدث هذا السجل كلما انتقل مطبوع من Legacy إلى Migrating أو من Migrating إلى Governed.
