# سجل حوكمة المطبوعات — Audit Registry

**الحالة المرجعية: دستور 1.15**

هذا السجل شرح بشري موازٍ للحالة البرمجية في `lib/print-governance.js`.

## Governed — خاضع بالكامل
### 1) طلب الإجازة
- المسار: `/print/leave/[id]`
- العائلة: HR
- آخر إصدار مراجع: **1.10**.
- يستخدم شبكة الدستور ومحرك الإجراءات المركزي.
- الإلكتروني واليدوي وضعان بديلان.
- التوقيع اليدوي يرث عنصر `ManualProcedureFields` المركزي: تاريخ بنقاط، مساحة كتابة، ثم `التوقيع:` بلا نقاط.

## Migrating — مربوط وجارٍ الاستكمال/الاعتماد
### 2) دورة المستخلص
- المسارات: `/print/claims/[id]`, `/print/claim/[id]`, `/print/invoice-request/[id]`
- العائلة: Projects/Finance
- أربعة مستندات من محرك واحد: محضر قياس، مطالبة مالية، إشعار استلام دفعة، مذكرة طلب فاتورة.
- انتقلت قواعد الشكل من CSS المحلي إلى طبقة `print-family-projects-finance` داخل `app/print/print-system.css` المركزي.
- صفحة المستخلص تمر عبر `ConstitutionPrintFrame`.
- خانات التوقيع اليدوي تستخدم `ManualProcedureGrid` + `ManualProcedureFields` المركزيين.
- جدول البنود يستخدم أدوار أعمدة دلالية ومحاور 48 موحدة: عمود التمتير يطابق حدّي عمود التسمية المظلل في المعلومات وعمود البنك/الآيبان في السداد، وفترة القياس لا تلتف وتحاذى يمينًا.
- المطبوع لا يعيد حساب قيمة السطر أو صافي المطالبة؛ يعرض القيم المحفوظة في المعاملة.
- المطلوب قبل Governed: مراجعة بصرية للحالات الأربع في Preview/PDF واعتماد المستخدم.

### 3) عرض السعر / جدول الكميات
- المسار: `/print/quote/[id]`
- العائلة: Commercial
- انتقل غلاف A4 والليترهيد والهوامش والترقيم إلى `ConstitutionPagedFrame` المركزي.
- يقيس محرك العرض كل بند كوحدة ذرية وينقله كاملًا عند حد الصفحة، ويكرر رأس الجدول مرة واحدة فقط في الصفحة الجديدة.
- عنوان المجموعة ينتقل مع أول بند تابع له متى أمكن، مع حواجز CSS دفاعية تمنع تجزئة الصفوف والكتل الرسمية.
- اجتاز عرض طويل من ثلاث صفحات واختبارات حدية آلية للبند الطويل وحد الصفحة وعنوان المجموعة والكتلة الأكبر من الصفحة.
- المطلوب قبل Governed: مراجعة جدول كميات، وختم وتوقيع مسحوبين.

### 4) تقرير الموظفين
- المسار: `/print/employees`
- العائلة: HR
- انتقل غلاف الصفحات والهوية والهوامش والترقيم إلى إطار الصفحات المتعددة الدستوري.
- بقي تقسيم الصفوف بالقياس الفعلي كما هو حتى لا تنقسم بيانات موظف بين صفحتين.

### 5) مجلس الإدارة والملاك
- المسار: `/print/board`
- العائلة: Governance
- يمر عبر `ConstitutionPrintFrame` والموازنة المركزية بدل بناء A4 والرأس والتذييل محليًا.

### 6) محرك المستندات العام
- المسار: `/print/[id]`
- العائلة: General Documents
- دخل إطار الصفحة والهوية والهوامش والموازنة المركزية دون تغيير بيانات القوالب أو منطقها.
- القوالب الطويلة والعقود متعددة الصفحات تراجع على دفعات قبل Governed.

## Legacy — مسجل لكنه خارج الحكم الكامل
### 7) العرض الوظيفي العام
- `/offers/[token]`
- شاشة عامة تفاعلية وليست نسخة طباعة دستورية بعد.

### 8) مسودة عقد المرشح
- `/contracts/[token]`
- شاشة عامة تفاعلية وليست نسخة طباعة دستورية بعد.

## ربط الحوكمة
- كل `/print` يمر عبر `PrintGovernanceBoundary`.
- كل مطبوع مسجل يأخذ `document`, `family`, `status`, `governance-version` آليًا.
- أي `/print` غير مسجل يصنف `unregistered` ولا يعتبر Governed.
- التعديلات العامة في الطبقة المشتركة.
- فروق العائلة داخل `.print-family-*` في الطبقة المركزية.
- فروق مستند واحد داخل `.print-doc-*` في الطبقة المركزية.

## محرك الخلايا والمعادلات — 1.15
- المصدر: `lib/cell-engine.js`.
- Money: خانتان عشريتان دائمًا.
- Quantity / Number / Percent: لا تقريب مالي تلقائي.
- `lib/form-engine.js` يستخدم نوع الخلية عند توفر Layout أو `result_type/target_type`.
- الهدف المرحلي: نقل المعادلات المالية والهندسية من الصفحات إلى هذا المحرك/لقطات الاعتماد.

## ترتيب الهجرة
1. مراجعة دورة المستخلص بالحالات الأربع وإغلاقها كـ Governed.
2. مراجعة تقارير الموظفين ومجلس الإدارة على بيانات قصيرة وطويلة.
3. مراجعة جدول الكميات والسحب في عرض السعر؛ تقسيم البنود متعدد الصفحات اجتاز المراجعة.
4. تقسيم القوالب الطويلة في محرك المستندات العام إلى صفحات دستورية.
5. إنشاء نسخ طباعة رسمية للعروض الوظيفية ومسودات العقود العامة.

يحدث هذا السجل عند انتقال أي مطبوع من Legacy → Migrating → Governed.
